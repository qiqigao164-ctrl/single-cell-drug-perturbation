# Single-cell drug perturbation mini-study — Python environment
# Recommended setup: Miniforge (conda-forge) + pip for the packages below.
# Note: pertpy pulls heavy JAX-based dependencies (jax, jaxlib, flax, ott-jax)
#       which have no conda-forge Windows build — install them via pip.

# --- single-cell stack (conda-forge or pip) ---
scanpy==1.11.5
anndata==0.12.19
pertpy==1.0.3

# --- differential expression & enrichment ---
pydeseq2==0.5.4
gseapy==1.3.1

# --- numerical / scientific ---
numpy==2.4.6
scipy==1.17.1
pandas==2.3.3
matplotlib==3.11.1
scikit-learn==1.9.0

# --- clustering ---
igraph==1.0.0
leidenalg==0.12.0

# --- notebooks ---
jupyterlab==4.6.3
nbformat==5.11.0
